#pragma once

#include "sdk/sdk.h"

struct draw 
{

    void DrawRect(int x, int y, int w, int h, bool filled, Color color) {

    }

    void PrintText(int x, int y, int size, HFont font, Color color, const char* text, ...) {

    }

    void DrawCircle(float x, float y, float r, float s, Color color) {

    }
};